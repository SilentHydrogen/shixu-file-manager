import { Project } from '@ohos/hvigor';
import { BaseOhosProjectTask } from './base/base-ohos-project-task.js';
/**
 * 清理跨平台应用中迁移的产物
 * （挂载到hvigor --sync任务上）
 *
 * @since 2022/11/24
 */
export declare class Clear extends BaseOhosProjectTask {
    constructor(project: Project);
    doAndroidAction(): Promise<void>;
    doIosAction(): Promise<void>;
    private clearAndroidProject;
    private clearIosProject;
    private deleteFilesWithDirs;
}
