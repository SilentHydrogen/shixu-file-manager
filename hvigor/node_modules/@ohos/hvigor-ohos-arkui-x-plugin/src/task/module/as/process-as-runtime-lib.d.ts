import { TargetTaskService } from '@ohos/hvigor-ohos-plugin/src/tasks/service/target-task-service.js';
import { BaseOhosModuleTask } from '../../base/base-ohos-module-task.js';
/**
 * 处理Android应用所需平台运行时依赖库
 *
 * @since 2023/05/08
 */
export declare class ProcessAsRuntimeLib extends BaseOhosModuleTask {
    private readonly armLibPath;
    private readonly arm64LibPath;
    private readonly adapterFilePath;
    constructor(taskService: TargetTaskService);
    initTaskDepends(): void;
    doTaskAction(): Promise<void>;
    copyRuntimeLib(libPath: string, archType: string): void;
}
