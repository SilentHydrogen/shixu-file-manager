import { Project } from '@ohos/hvigor';
import { BaseOhosProjectTask } from './base/base-ohos-project-task.js';
/**
 * 跨平台应用打包（Android）
 *
 * @since 2022/11/24
 */
export declare class PackageAsApp extends BaseOhosProjectTask {
    private readonly log;
    private readonly androidProjectDir;
    private readonly buildMode;
    private readonly buildTool;
    constructor(project: Project);
    initTaskDepends(): void;
    doTaskAction(): Promise<void>;
    buildApp(): Promise<void>;
    buildLibrary(): Promise<void>;
    executeBuildCommand(command: string): Promise<void>;
    macOrLinuxForGradlewAuthorization(): Promise<void>;
}
