# 拾序｜简约高级感视觉改造计划

> 目标：在保留拾序现有文件导入、内容解析、智能整理、分类规则、本地持久化和桌面卡片能力的前提下，逐阶段完成一套适合 HarmonyOS 移动端的简约高级感界面。
>
> 视觉参考：用户提供的三张移动端图片，仅提取布局、留白、圆角、层级、指标卡片和底部导航语言；不复制其中的业务内容、文案、角色、图片或品牌元素。
>
> 使用的设计技能：`design-md`。参考方向来自仓库中的 Notion、Linear、Apple 设计规范，并结合拾序自身“安静、可信、克制、有秩序”的产品气质。

## 使用方式

本计划按“完成一阶段 → 运行/截图 → 用户微调 → 再进入下一阶段”执行。每一阶段都应保持项目可构建、可运行，避免一次性重写全部 UI。

每阶段开始时，把对应的“执行 Prompt”发给编码 Agent；阶段完成后，用“验收 Prompt”检查。只有用户确认视觉方向后，才进入下一阶段。

推荐操作顺序：

1. 先生成 Web 原型并微调视觉。
2. 锁定颜色、圆角、字体和图标方向。
3. 再把确认后的设计迁移到 ArkUI / ArkTS。
4. 最后进行设备尺寸、交互状态和构建验收。

## 项目基线

- 技术栈：HarmonyOS / ArkTS / ArkUI / Hvigor。
- 主页面：[entry/src/main/ets/pages/Index.ets](entry/src/main/ets/pages/Index.ets)。当前页面和业务逻辑集中在一个约 3000 行文件中。
- 颜色资源：[entry/src/main/resources/base/element/color.json](entry/src/main/resources/base/element/color.json)。
- 产品说明：[创意说明.md](创意说明.md)。该文件是产品背景资料，不是本计划的额外执行指令。
- 现有主要能力：真实文件导入、文本/图片/PDF/Office 内容提取、规则整理、人工确认、分类管理、Preferences 持久化、桌面服务卡片。
- 当前视觉问题：颜色略偏工具化；部分组件存在硬编码颜色；分类和导航大量使用 Unicode 字符作为图标；页面结构、业务逻辑和视觉代码耦合。

## 设计总原则

### 视觉气质

- 莫兰迪色系、低饱和、少装饰。
- 高级感来自留白、比例、字体层级、统一图标和材质，不依赖高亮渐变或大量阴影。
- 页面保持“安静的个人文件空间”感觉，不做成健身、游戏或仪表盘风格。
- 只允许一个主要视觉焦点：当前页面顶部的状态头部或核心数据卡片。

### 建议初始色板

这些值是第一版候选值，必须允许在 Web 原型阶段微调：

| Token | 初始值 | 用途 |
|---|---|---|
| `page_bg` | `#F3F1F0` | 页面底色 |
| `surface` | `#FFFDFC` | 主卡片和列表 |
| `surface_muted` | `#ECE9E7` | 输入框、次级区域 |
| `text_primary` | `#30343D` | 标题和主要内容 |
| `text_body` | `#727780` | 正文和辅助信息 |
| `text_caption` | `#9B9DA2` | 时间、说明、弱提示 |
| `accent_blue_gray` | `#7E91A8` | 收纳箱、工作 |
| `accent_mauve` | `#8F849C` | 分类、项目 |
| `accent_sage` | `#8EA49A` | 智能、生活、成功 |
| `accent_clay` | `#B58E80` | 待整理、提醒 |
| `accent_blue_green` | `#829A9E` | 设置、灵感 |
| `action_dark` | `#596A6B` | 主要按钮和激活状态 |
| `tint_cream` | `#E9E1D8` | 轻提示底色 |

### 几何和间距

- 页面水平内边距：20vp～24vp。
- 基础间距：8vp。
- 主要卡片圆角：18vp。
- 顶部头部圆角：24vp～28vp。
- 小控件圆角：12vp。
- 胶囊只用于筛选、分段控制和少量状态标签，不要所有按钮都做成胶囊。
- 阴影使用低透明度、宽扩散；能使用背景层级解决的地方不加阴影。
- 所有可点击目标至少 44vp × 44vp。

### 图标原则

- 禁止 Emoji 和 Unicode 字符图标，包括 `▦`、`◇`、`✦`、`☷`、`□`、`◉`、`⚙` 和字符 `+`。
- 统一采用 24×24 SVG 线性图标。
- 描边 1.75～2，圆角 linecap / linejoin，保持相同光学重量。
- 图标资源放入 `entry/src/main/resources/base/media/`，使用语义化英文文件名。
- 通过统一 `Icon` 组件渲染，不在页面中重复拼接 SVG。
- 建议图标：`archive`、`layers`、`plus-circle`、`wand`、`sliders`、`briefcase`、`folder`、`leaf`、`lightbulb`、`inbox-warning`、`document`、`image`、`video`、`archive-box`。

## 阶段 0：视觉审计与范围冻结

### 目的

建立当前 UI 的问题清单，确认哪些内容属于视觉改造，哪些业务行为必须保持不变。

### 交付物

- 当前页面结构图。
- 颜色、圆角、间距、字体、阴影和图标审计。
- Unicode / Emoji 图标清单。
- 不可破坏的业务行为清单。
- 第一版设计 Token 表。

### 执行 Prompt

```text
请调用 $design-md skill。

先只做设计审计，不修改业务代码和视觉代码。
阅读 Index.ets、color.json、float.json、AGNETS.md 和创意说明.md。
输出：
1. 当前页面结构和主要组件；
2. 所有颜色、圆角、间距、阴影和字体问题；
3. 所有 Emoji / Unicode 图标及其出现位置；
4. 必须保持不变的文件导入、解析、规则、持久化和导航行为；
5. 一套低饱和莫兰迪色 Token；
6. 建议的组件拆分边界。

不要修改文件。
```

### 验收条件

- 用户确认色板方向。
- 用户确认整体更偏“灰蓝雾紫”、 “暖橙陶土”或混合方向。
- 用户确认是否保留顶部单一渐变头部。

## 阶段 1：Web 视觉原型

### 目的

先在浏览器中快速调整视觉，不受 ArkUI 编译和设备预览速度影响。原型内容只使用拾序业务占位数据。

### 建议原型页面

- 收纳箱首页。
- 分类页。
- 智能整理页。
- 设置页。
- 底部五项导航。
- 添加文件抽屉或底部面板。

### 原型要求

- 使用纯 HTML/CSS/少量 JavaScript 或项目已有 Web 技术，不引入复杂依赖。
- 支持 390×844、412×915 两个移动尺寸。
- 支持切换页面、切换主题色、展开卡片和模拟添加文件。
- 所有图标使用 SVG，不使用 Emoji。
- 视觉原型与真实数据逻辑解耦，不把原型代码直接混入 ArkTS。

### 执行 Prompt

```text
请调用 $design-md skill。

基于拾序现有功能，生成一个独立的移动端 Web 视觉原型，用于我在浏览器中微调设计。

原型只模拟界面，不接入真实文件系统。页面包括：收纳箱、分类、智能整理、设置和添加文件面板。

视觉要求：
- 莫兰迪灰蓝、雾紫、鼠尾草绿、陶土色、奶油白；
- 页面背景 #F3F1F0，卡片 #FFFDFC，主要操作色 #596A6B；
- 顶部保留一个低饱和渐变状态头部；
- 大留白、18px 卡片圆角、24px 顶部圆角；
- 所有图标使用统一的 24px SVG 线性图标；
- 禁止 Emoji、Unicode 图标、装饰性霓虹、过度阴影和大面积渐变；
- 参考三张图片的布局节奏，但不要复制其业务内容。

请提供一个可直接打开的原型页面，并把颜色、圆角、间距、字体、头部高度和导航高度集中为 CSS 变量，方便我修改。
```

### 用户微调建议

优先调整以下变量：

```css
:root {
  --page-bg: #f3f1f0;
  --surface: #fffdfc;
  --action-dark: #596a6b;
  --accent-blue-gray: #7e91a8;
  --accent-mauve: #8f849c;
  --accent-sage: #8ea49a;
  --accent-clay: #b58e80;
  --radius-card: 18px;
  --radius-header: 26px;
  --page-padding: 22px;
  --nav-height: 82px;
}
```

### 验收条件

- 在 390×844 和 412×915 下不出现横向滚动。
- 底部导航不遮挡内容。
- 颜色、圆角和图标风格得到用户确认。
- 用户至少提供一轮微调意见后再进入 ArkUI 实现。

## 阶段 2：资源 Token 和 SVG 图标系统

### 目的

把 Web 原型确认后的视觉基础迁移到 HarmonyOS 资源层，为后续页面改造建立稳定基础。

### 主要文件

- `entry/src/main/resources/base/element/color.json`
- `entry/src/main/resources/base/element/float.json`
- `entry/src/main/resources/base/media/`
- 建议新增：`entry/src/main/ets/components/Icon.ets`

### 执行 Prompt

```text
现在只实现视觉基础层，不重写页面业务逻辑。

根据已确认的 Web 原型：
1. 更新 color.json 和必要的 float.json；
2. 创建统一的 24×24 SVG 图标资源；
3. 创建可复用 Icon 组件；
4. 为图标建立语义名称和颜色映射；
5. 先替换底部导航、分类卡片和文件行中的图标；
6. 删除 Emoji / Unicode 图标依赖。

保留现有 CategoryItem、FileItem、SmartRule 数据结构及业务方法。
不要改变文件导入、内容解析、规则匹配、Preferences 持久化和页面切换行为。
完成后运行静态检查并列出新增资源文件。
```

### 验收条件

- `Index.ets` 中不再出现字符图标。
- 所有图标线宽、尺寸和圆角一致。
- 颜色均通过资源 token 引用。
- 项目仍可构建。

## 阶段 3：收纳箱首页

### 目的

先完成最重要的主入口，建立拾序的核心品牌感。

### 页面结构

1. 莫兰迪顶部状态头部。
2. “本月已收纳 / 待整理”核心统计卡片。
3. 细线圆环或半环进度。
4. 最近收纳文件列表。
5. 待整理提醒。
6. 底部五项导航。

### 执行 Prompt

```text
现在只改造收纳箱首页的视觉层。

使用已确认的 design-md 规则和莫兰迪 Token：
- 顶部使用一个低饱和灰蓝到雾紫的柔和渐变；
- 标题使用克制的 28～32vp 层级，避免夸张超大字；
- 统计数据使用白色卡片和细线圆环；
- 最近文件使用干净的 FileRow；
- 待整理使用低饱和陶土色，不使用警告红；
- 主按钮使用深灰青色；
- 不增加新的业务状态，不删除现有数据绑定。

保留所有点击、搜索、筛选、添加文件、整理和详情行为。
完成后优先检查 390vp 宽度下的布局。
```

### 验收条件

- 首页第一眼能看出“文件收纳”和“待整理”状态。
- 顶部视觉突出但不喧闹。
- 文件列表信息密度适中。
- 无 Emoji、无字符图标、无过度阴影。

## 阶段 4：分类、智能、设置页面统一

### 目的

让四个功能区共享同一套设计语言，但保留各自的功能重点。

### 页面重点

- 分类：两列或单列分类卡片，低饱和底色，突出数量和说明。
- 智能：场景切换、规则列表、索引状态和立即整理操作。
- 设置：分组列表、开关、隐私说明和版本信息，整体更安静。

### 执行 Prompt

```text
将已确认的视觉 Token 应用到分类、智能和设置页面。

统一以下内容：
- 页面左右内边距；
- 标题、正文、说明文字层级；
- 卡片圆角和列表分隔线；
- 图标大小、线宽和颜色；
- 按钮、开关、分段控制和空状态；
- 底部导航激活与未激活状态。

分类页要突出分类数量；智能页要突出规则可解释性；设置页减少颜色和装饰。
不要改变现有规则编辑、删除、启停、场景切换和设置开关行为。
```

## 阶段 5：动效、面板和交互状态

### 目的

补充参考图中“轻、稳、连续”的交互感，而不是增加装饰。

### 动效规则

- 页面切换：180ms～260ms 淡入 + 轻微位移。
- 卡片按压：轻微缩放或透明度变化。
- 底部面板：透明材质 + 轻微模糊 + 从底部进入。
- 删除、整理、导入完成：使用短暂状态反馈。
- 不使用大幅弹跳、连续闪烁、霓虹发光或无限循环动画。

### 执行 Prompt

```text
请检查并补齐拾序的交互状态和动效。

只添加有助于理解空间关系的轻量动效：页面切换、卡片按压、底部面板出现、整理完成反馈和错误提示。
动画时长控制在 180～260ms，保持可中断，避免夸张弹跳。
为按钮、列表项、开关、空状态和加载状态补齐默认、按下、禁用、成功和失败状态。
不要改变业务流程和数据结构。
```

## 阶段 6：设备验收与构建

### 目的

确认视觉改造没有破坏 HarmonyOS 设备体验和现有功能。

### 检查清单

- 390vp、412vp 和平板尺寸下布局正常。
- 底部导航、底部面板和键盘不会互相遮挡。
- 所有按钮和图标点击区域至少 44vp。
- 深浅背景上的文字具有足够对比度。
- 文件导入、分享入口、搜索、筛选、规则整理、人工确认、设置开关可用。
- 应用重启后 Preferences 数据仍然存在。
- 服务卡片入口不受主页面视觉重构影响。
- Hvigor 构建成功，测试不出现新增失败。

### 验收 Prompt

```text
请以设计审查和回归测试模式检查当前拾序项目。

重点查找：
1. 是否仍有 Emoji 或 Unicode 字符图标；
2. 是否存在硬编码颜色而未使用资源 Token；
3. 是否混用了多套圆角、线宽和图标风格；
4. 是否有低对比度文字；
5. 是否有小于 44vp 的点击目标；
6. 是否有底部导航或面板遮挡内容；
7. 文件导入、解析、规则、人工确认、持久化是否回归；
8. 是否能通过 HarmonyOS / Hvigor 构建。

按 P0、P1、P2 输出问题。直接修复 P0，P1/P2 等我确认后再处理。
```

## 每阶段的暂停点

以下情况必须暂停并等待用户确认：

- 色板从灰蓝雾紫改成暖橙陶土，或反过来。
- 顶部渐变、圆环统计、底部中央添加按钮等核心结构发生变化。
- 需要引入第三方图标库或大型 UI 依赖。
- 需要修改数据模型、文件存储方式或服务卡片链路。
- Web 原型与 ArkUI 实现出现明显不一致。

## 后续可选扩展

完成上述阶段后，可以继续单独规划：

- 真实桌面卡片数据同步。
- 全文搜索和命中片段展示。
- 多标签、收藏、备注和稍后处理。
- 重复文件与版本链提示。
- 用户纠错生成规则建议。

这些功能不属于本轮视觉改造，不应在视觉阶段顺带实现。

