# 「拾序」创意说明书

> 让每个文件，都有自己的位置。

## 一、创意概述

「拾序」是一款面向 HarmonyOS 的本地智能文件收纳应用。应用内的核心空间叫“收纳箱”：用户把散落在设备中的文档、图片和资料放入收纳箱，软件读取用户明确授权的文件，根据文件名、格式与正文内容执行整理规则，把文件归入“工作、项目、生活、灵感”等有意义的标签。

它解决的不是“设备能不能保存文件”，而是三个更常见的问题：

1. 文件保存后散落在不同位置，过一段时间便难以找回；
2. 手工建立目录、移动文件和反复归类的成本过高；
3. 普通文件管理器知道文件在哪里，却不知道文件对用户意味着什么。

「拾序」希望在“保存文件”和“再次使用文件”之间增加一层轻量秩序，让用户只负责最终判断，把重复整理交给规则。

## 二、名称与品牌表达

“拾”代表从信息洪流中拾起值得保留的内容，“序”代表为内容恢复秩序。两个字共同表达一种温和的产品态度：软件不是强迫用户一次性整理整个设备，而是从每一次主动收纳开始，逐步建立个人知识与文件秩序。

- 产品名称：拾序
- 核心功能空间：收纳箱
- 宣传语：让每个文件，都有自己的位置
- 核心动作：拾取、理解、归纳、找回
- 产品气质：安静、可信、克制、有秩序

## 三、核心创意

传统文件管理以目录和路径为中心，例如“下载/文档/其他”；「拾序」以文件的用途和意义为中心，例如“课程作业”“项目交付”“旅行资料”。

核心体验链路为：

**选择真实文件 → 提取可理解内容 → 执行透明规则 → 自动归入标签 → 搜索或从分类找回**

这一方案的关键不是使用一个不可解释的“万能 AI”，而是建立一种可信的人机协作：

- 系统处理能够确定的重复工作；
- 无法确定的文件进入“待整理”，不强行猜测；
- 用户手动归纳后，结果受到保护，不会被下一次自动整理覆盖；
- 每条规则都能查看、编辑、启用或停用，整理依据始终可见。

## 四、当前软件已经实现的能力

本节依据当前项目代码重新核对，描述的是现有实现，而不是未来设想。

### 4.1 从系统批量导入真实文件

- 使用系统文件选择器获取用户授权的文件 URI；
- 单轮支持选择最多 100 个文件，并允许再次选择后合并；
- 自动排除同一 URI 的重复导入；
- 读取文件名、扩展名和真实文件大小；
- 根据扩展名识别文档、图片、音频、视频、压缩包和设计文件；
- 已归纳文件可以调用系统查看能力打开。

这使「拾序」不是一套只有演示数据的 UI，而是具备真实文件入口与打开链路的原型产品。

### 4.2 多格式本地内容解析

软件已建立分层内容提取能力，最多保留约 32,000 个字符用于规则匹配和内容预览：

| 文件类型 | 当前处理方式 |
| --- | --- |
| TXT、Markdown、CSV、JSON、XML、HTML、日志及常见源码 | 直接读取文本 |
| JPG、PNG、HEIC、GIF、BMP、WebP 等图片 | 调用端侧文字识别能力执行 OCR |
| DOCX、PPTX、XLSX | 在缓存目录解压 Office Open XML，抽取正文、幻灯片、备注或表格文字 |
| PDF | 优先读取文字层；页面没有文字时尝试 OCR；当前最多处理前 30 页 |
| 其他格式 | 不读取正文，仍可依据标题和扩展名整理 |

解析过程会记录“解析状态”和“解析来源”，用户在文件标签面板中可以看到正文预览或失败原因。临时解析目录在任务完成后删除。

### 4.3 透明、可编辑的智能整理规则

每条规则包含以下条件：

- 标题关键词；
- 正文关键词；
- 文件格式；
- 目标标签。

同一个条件中的多个关键词以逗号分隔，命中任意一个即可；当一条规则同时填写多个条件时，这些条件需要共同满足。系统按规则顺序查找第一条匹配规则，命中后完成归类，否则放入“待整理”。

用户可以：

- 新建自定义规则；
- 修改默认规则或自定义规则；
- 单独启用、停用某条规则；
- 暂停整个自动整理功能；
- 修改规则后立即整理现有文件；
- 对全部文件重新解析正文并再次整理。

### 4.4 场景化规则预设

软件为四种典型使用方式准备了不同的默认规则：

- 学生：课程、笔记、讲义、作业、实验和学习文档；
- 摄影师：原片、照片、视频素材和灵感参考；
- 程序员：项目源码、README、构建文件、技术文档和交付压缩包；
- 通用：工作文档、生活资料、图片素材和项目文件。

切换使用方式会替换默认规则，但保留用户创建的自定义规则。这一设计兼顾“开箱即用”和“长期个性化”。

### 4.5 语义化分类与人工兜底

软件内置“工作、项目、生活、灵感、待整理”五个标签，并支持创建自定义分类、选择颜色和图形标记。

人工归纳具有特殊意义：文件一旦被用户手动指定标签，就会被标记为人工结果，后续自动整理不会随意改写。对于无法判断的文件，软件先展示“归纳”或“打开文件”的操作面板，让用户决定下一步，而不是隐藏错误。

### 4.6 收纳箱、分类、智能与设置四个功能区

- 收纳箱：展示文件、标签筛选、搜索、自动整理状态和本月收纳数量；
- 分类：查看各标签的文件数量，点击标签返回收纳箱查看结果；
- 智能：选择使用方式、管理规则、查看索引状态并重新解析；
- 设置：开关自动整理、说明索引范围和展示版本信息；
- 底部中央“添加”：随时打开批量文件导入面板。

当前搜索逻辑实际支持文件名和标签；界面中的“类型”搜索提示可在后续版本补齐。

### 4.7 本地状态持久化

文件索引、解析结果、分类、规则、使用方式和自动整理开关会写入本地 Preferences。应用重新启动后可以恢复用户的收纳状态，而不是每次从空白开始。

### 4.8 HarmonyOS 桌面服务卡片原型

项目已配置两张 2×2 服务卡片并完成视觉界面：

- 最近文件：展示最近收纳内容的快捷入口；
- 快捷分类：展示常用标签及数量。

卡片已经能够添加到桌面，采用半透明、圆角和轻阴影的玻璃感设计。当前卡片展示内容仍是静态示例，FormExtensionAbility 尚未把应用内的真实索引数据同步给卡片，精确分类跳转参数也尚未在主 Ability 中消费。因此，它属于“视觉与入口已完成、动态数据链路待完成”的功能。

### 4.9 视觉与动效语言

界面以浅灰蓝背景、低饱和标签色、白色圆角卡片和大留白构成，强调安静而非工具软件常见的密集感。底部操作面板使用半透明模糊材质，文件按压、页面切换和面板出现采用轻量弹性动画，为操作建立空间连续性。

不同颜色不是装饰，而承担分类识别作用：蓝色偏工作、紫色偏项目、绿色偏生活、青色偏灵感、暖黄色偏待整理。用户可以凭颜色快速形成位置记忆。

## 五、软件的主要创新点

### 5.1 从“文件路径”转向“文件意义”

用户不必记住文件存放在哪一层目录，只需记得它属于哪个生活或工作场景。软件把文件系统语言翻译成自然认知语言。

### 5.2 从“只看文件名”升级为“理解文件正文”

图片 OCR、PDF 文本与扫描页识别、Office 正文抽取，使规则能够依据文件里面写了什么进行整理。例如文件名只是“IMG_20260720.jpg”，但图片里出现“课程表”，仍可通过正文规则识别其用途。

### 5.3 可解释智能，而不是黑盒分类

规则本身就是解释。用户能够看到“标题含什么、正文含什么、格式是什么、最终去哪里”，也能看到文件的解析来源。可解释性让智能整理适合处理个人资料，而不只是做一次性的展示效果。

### 5.4 自动化与人工决定之间有明确边界

自动规则只处理确定性较高的内容；不确定内容留在“待整理”；人工结果不会被自动化覆盖。这比追求百分之百自动分类更可靠，也更尊重用户。

### 5.5 本地优先的隐私方案

当前实现围绕用户授权 URI 和设备端能力工作，不需要把原文件上传到服务器。应用只索引用户主动选择的内容，符合个人文件工具对隐私与控制感的要求。

### 5.6 利用 HarmonyOS 桌面入口缩短找回路径

“最近文件”和“快捷分类”服务卡片把收纳结果带到桌面。产品由一个需要主动打开的 App，进一步变成随手可见、一步可达的系统级入口。

## 六、典型使用场景

### 场景一：学生整理课程资料

学生一次导入老师发送的 PDF 讲义、PPTX 课件、作业截图和实验文档。软件提取正文，依据课程名、“作业”“实验”等关键词自动归类；没有明确特征的文件进入待整理。之后学生只需打开课程标签即可集中查看。

### 场景二：职场人士整理项目交付物

用户建立规则：“正文包含客户名称，格式为 PDF、DOCX 或 PPTX，归入项目”。导入会议纪要、方案和交付稿后，软件自动完成初步归纳。用户手动修正一次后，该文件标签不再被自动规则覆盖。

### 场景三：从图片中找回信息

用户导入海报、课程表、发票或带文字的截图。应用通过 OCR 提取文字，内容不仅能够参与规则匹配，还可在文件标签面板中预览，减少逐张打开确认的成本。

### 场景四：程序员整理开发资料

程序员使用预设规则，把源码、README、构建配置归入项目，把接口说明与会议文档归入工作，把压缩交付包归入项目。软件把扩展名规则和正文关键词结合，降低混放概率。

## 七、后续可发展的创意

以下内容属于未来规划，当前版本尚未完整实现。

### 7.1 让桌面卡片使用真实数据

通过 FormBindingData 同步最近文件与分类统计，点击某个标签后直接打开对应筛选结果；增加待整理数量与“今日需要处理”状态，使卡片成为真正的快捷工作台。

### 7.2 全文搜索与搜索证据

把当前文件名、标签搜索扩展为正文全文搜索，并显示命中片段。例如搜索“无障碍设计”时，告诉用户该词出现在某份 PDF 的第 6 页，而不是只返回一个文件名。

### 7.3 自然语言找文件

允许用户输入“上个月那份包含登录接口的方案”或“图片里写着高铁时间的截图”。系统把时间、格式、标签和正文语义组合成检索条件，同时展示匹配依据。

### 7.4 从用户纠错中生成规则

当用户多次把相似文件从“待整理”归入同一标签时，软件可提出一条明确的新规则，例如“检测到你常把标题含‘实验报告’的 DOCX 归入工作，是否创建规则？”只有用户确认后才启用。

### 7.5 多标签与文件关系

允许一份文件同时属于“项目”和“工作”，并识别需求、草稿、会议记录、终稿之间的关联。最终可把散落文件组织为一条项目故事线，而不仅是一组平铺列表。

### 7.6 重复文件与版本链

识别完全重复文件、近似图片及“最终版、最终版2、真的最终版”等版本关系。软件只给出可预览、可撤销的建议，不主动删除原文件。

### 7.7 带目的的收纳

收纳时可附加“稍后阅读、等待回复、某日前提交、旅行时使用”等意图。服务卡片在合适时间重新呈现文件，让收纳从静态保存升级为下一步行动。

### 7.8 跨设备索引与接续

利用 HarmonyOS 多设备能力，在手机、平板和电脑之间同步标签与索引，并标明原文件所在设备。默认先同步轻量元数据，原文件传输由用户单独决定。

## 八、建议的产品迭代路线

### 第一阶段：把现有核心闭环做稳

1. 完成服务卡片真实数据同步与定向跳转；
2. 补齐按文件类型和正文搜索，修正界面提示与实际能力的差异；
3. 增加删除索引、批量改标签、规则排序和规则删除；
4. 完善授权失效、超大文件、加密文件和解析失败提示；
5. 对已保存的正文索引增加清除入口和更明确的隐私说明。

### 第二阶段：提高找回效率

1. 增加全文索引、命中片段和组合筛选；
2. 增加多标签、收藏、备注和稍后处理；
3. 增加分享面板入口，让聊天附件或网页资料直接进入拾序；
4. 增加重复文件与版本关系提示。

### 第三阶段：形成个人整理助手

1. 根据用户确认与纠错提出规则建议；
2. 引入可解释的端侧语义检索和自动摘要；
3. 构建项目故事线与时间提醒；
4. 在用户可控的前提下提供跨设备索引接续。

## 九、设计与隐私原则

1. **主动授权**：只读取用户明确选择的文件。
2. **本地优先**：能够在设备端完成的解析与判断不上传云端。
3. **结果可解释**：显示解析来源、规则条件和归类去向。
4. **人工优先**：用户手动归纳的结果高于自动规则。
5. **失败可见**：解析失败或无法判断时明确提示，不悄悄给出错误结果。
6. **操作可撤销**：未来涉及移动、同步或清理文件时必须提供确认与恢复机制。
7. **低打扰**：提醒、报告和动画都服务于找回效率，不制造新的管理负担。

## 十、作品展示用简介

### 一句话版本

**「拾序」是一款能读取 PDF、Office 文档和图片文字，并用透明规则自动归纳真实文件的 HarmonyOS 本地智能收纳应用。**

### 答辩版本

传统文件管理器解决“文件存在哪里”，「拾序」进一步解决“文件为什么被保存，以及以后怎样找回来”。用户把真实文件加入收纳箱后，应用在本地提取文本、Office 文档、PDF 和图片 OCR 内容，再根据可查看、可编辑的规则完成标签归类。无法判断的内容留在待整理，人工修正后的标签不会被自动覆盖。它把重复工作交给系统，把最终决定留给用户，并通过 HarmonyOS 服务卡片把最近文件与常用分类带到桌面。

---

文档版本：2.0  
重新核对日期：2026 年 7 月 20 日  
核对范围：当前 ArkTS 源码、模块与服务卡片配置、项目内实机截图  
状态说明：第四节为当前实现；第七、八节为未来创意与迭代建议。
